import { curry } from '../fn/curry';
import { isOk } from './is-ok';
import { withCatch } from './lib/with-catch';
/** @tags effect */
export const tap = curry(withCatch(function tap(fn, res) {
    if (isOk(res))
        fn(res.value);
    return res;
}), 2);
