import type { Gen, GenYield } from '../fn/types';
/**
 * Consume a generator and `yield` only the final result.
 *
 * @tags generator
 */
export declare function takeLast<T extends Gen<any>>(gen: T): Gen<GenYield<T>>;
