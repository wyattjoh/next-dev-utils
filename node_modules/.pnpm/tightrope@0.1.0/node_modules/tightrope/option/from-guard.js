import { none, Some } from '.';
import { curry } from '../fn/curry';
/** @tags option, wrap, invoke */
export const fromGuard = curry(function fromGuard(guard, value) {
    return guard(value) ? new Some(value) : none;
}, 2);
