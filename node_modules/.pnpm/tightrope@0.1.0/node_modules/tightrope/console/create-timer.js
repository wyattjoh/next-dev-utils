/**
 * ...
 *
 * @tags console
 */
export function createTimer(id) {
    return {
        start() {
            console.time(id);
        },
        stop() {
            console.timeEnd(id);
        },
        log(msg) {
            return () => console.timeLog(id, msg);
        },
    };
}
