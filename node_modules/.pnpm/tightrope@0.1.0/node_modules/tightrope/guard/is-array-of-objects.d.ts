/**
 * Asserts that a value is an `Array` containing only `Object` values.
 *
 * ## Examples
 *
 * ```ts
 * isArrayOfObjects([{}, new Object()]);
 * ```
 *
 * @tags guard, arrays, objects
 */
export declare const isArrayOfObjects: (values: unknown) => boolean;
