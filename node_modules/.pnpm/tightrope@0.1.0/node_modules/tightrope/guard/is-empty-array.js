import { isArrayOfSize } from './is-array-of-size';
/**
 * Asserts that a value is a valid `Array` containing no items.
 *
 * @tags guard, arrays, array-length, emptiness
 */
export const isEmptyArray = (value) => isArrayOfSize(0, value);
